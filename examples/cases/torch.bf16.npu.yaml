# Torch NPU bfloat16 device-tensor contract sample.
cases:
  - id: 213
    operator: {name: torch.abs}
    invocation: {api: torch.abs, api_type: function, executor: torch}
    oracle: {comparison: allclose, reference_executor: torch, tolerance: {atol: 1.0e-2, rtol: 1.0e-2}}
    generator: default
    parameters:
      - {name: input, kind: tensor, dtypes: [bf16], shape: {dims: [2, 2]}, values: [[1.00390625, -2.0078125], [3.015625, -4.0]]}
