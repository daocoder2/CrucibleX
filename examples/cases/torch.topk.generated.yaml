# Declarative topk contract sample with legal and automatic invalid variants.
cases:
  - id: 308
    operator: {name: torch.topk}
    invocation:
      api: torch.topk
      api_type: function
      executor: torch
      binding: {mode: keyword, names: [input, k, dim, largest, sorted]}
    generator: default
    parameters:
      - {name: input, kind: tensor, dtypes: [fp32], shape: {dims: [2, 5]}}
      - {name: k, kind: attribute, dtypes: [int64], values: 3}
      - {name: dim, kind: attribute, dtypes: [int64], values: 1}
      - {name: largest, kind: attribute, dtypes: [bool], values: true}
      - {name: sorted, kind: attribute, dtypes: [bool], values: true}
    generation: {invalid_count: 2}
    oracle: {comparison: allclose, reference_executor: torch}
