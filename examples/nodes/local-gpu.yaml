# Single-host GPU execution on the machine that owns CUDA devices.
nodes:
  - name: local-gpu
    host: 127.0.0.1
    role: candidate
    allowed_tasks: [accuracy, run, performance_device, memory_device]
    devices:
      - id: 0
        backend: gpu
