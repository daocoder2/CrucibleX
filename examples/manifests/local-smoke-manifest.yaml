version: 1
kind: manifest
task:
  name: local-smoke
lanes:
  - name: cpu-smoke
    kind: contract
    backends: [cpu]
    cases:
      - include: ../cases/torch.abs.yaml
runtime:
  allow_generated_cases: true
  allow_invalid_cases: false
reporting:
  emit_case_index: true
  emit_lane_index: true
